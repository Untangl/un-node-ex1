{
  "name": "un-node-ex1",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.ts",
  "dependencies": {
    "axios": "^1.3.3",
    "table": "^6.8.1"
  },
  "scripts": {
    "start": "npx nodemon",
    "go": "rimraf ./build && tsc && node build/app.js",
    "build": "rimraf ./build && tsc",
    "run": "npm run build && node build/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.18.12",
    "nodemon": "^2.0.20",
    "rimraf": "^4.1.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
